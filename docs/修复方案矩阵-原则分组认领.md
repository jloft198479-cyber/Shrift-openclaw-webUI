# 修复方案矩阵 — 原则分组认领

> 基于两轮审查的 19 个问题（P0×6 + P1×6 + P2×5 + P3×2），按 5 条架构原则重新分组。
> 目的：让团队按「原则」而非「文件」认领，确保每组修复都触及同一类根因，避免头痛医头。
> 编制日期：2026-06-17
> 修订日期：2026-06-17（结合项目实际校准 + 回滚策略）

---

## 零、回滚策略（修复前必读）

**铁律：每个批次动手前，先 git commit + push，建立回滚点。**

### 修复前备份流程

```powershell
# 1. 确认当前工作区状态
git status

# 2. 提交当前所有改动（含未跟踪文件）作为修复前基线
git add -A
git commit -m "chore: 修复前基线快照（批次N前）"

# 3. 推送到 GitHub 远程备份
git push origin main

# 4. 记录当前 commit hash，万一需要回滚用
git log --oneline -1
```

### 回滚流程（某批次修复出问题时）

```powershell
# 方案A：整体回滚到修复前基线（推荐）
git reset --hard <修复前基线commit hash>

# 方案B：只撤销某个批次的 commit（保留其他批次）
git revert <问题批次的commit hash>

# 方案C：从 GitHub 拉取最后一次正确的版本
git fetch origin
git reset --hard origin/main
```

### 分批提交策略

- **每个批次独立 commit**，不混在一起
- commit message 格式：`fix(批次N): P0-2 切换清理dispatching + P1-2 WorkspacePicker泄漏`
- **每批修复完先测试**，测试通过再 commit + push
- 测试不通过就 `git checkout -- <文件>` 回退该文件，不 commit

### 当前状态提醒

当前工作区有未提交的改动（workspace 功能、roster-sync 等），**修复前必须先提交这些改动**，否则 git reset 会丢失工作。

---

## 零·一、项目实际校准（结合 OpenClaw 原生能力）

第三方审查基于代码本身，但不了解 OpenClaw Gateway 的原生能力。以下校准已结合项目实际：

### 校准 1：P0-1 历史截断 — Gateway 已有兜底

OpenClaw Gateway 已内置两层上下文管理：
- **ContextPruning**（已配置 `openclaw.json` 第 190-203 行）：`cache-ttl` 模式，TTL 1h，`keepLastAssistants: 3`，softTrim 4000 字符
- **Compaction**（默认开启）：接近上下文窗口上限时自动压缩，context-overflow 错误时自动重试

**影响**：客户端 `buildApiMessages` 全量发送 → Gateway 会自动修剪。Token 爆炸的终极风险被 Gateway 兜住了。
**但不影响**：图片 dataUrl 重发问题——Gateway 文档明确"图像块永远不会被修剪/清除"，dataUrl 每轮重发的浪费无法被覆盖。

**结论**：P0-1 拆成两半，优先级不同：
- 图片 dataUrl 剥离 → **必须修**（Gateway 管不了）
- 历史截断 → **降级为可选优化**（减少网络传输，但不紧急）

### 校准 2：P0-3 严重度下调

实际代码 `session-sync.js:94` 只对主 Agent session（不含 `:subagent:`）触发 `_scheduleRead`。多个子 Agent 结果通过主 Agent session 文件增量统一读取，不是每个子 Agent 各自触发。并发覆盖问题没有原报告描述的那么严重。

**结论**：P0-3 严重度从 🔴 降为 🟡。修复方案（Map 队列）仍然正确，但不紧急。

### 校准 3：P0-6 轻量方案需补充去重说明

"clients.length=0 不推进 offset"后，下次有客户端时 `_doRead` 会从旧 offset 重读，产生重复消息。前端有 `_announcedOffsets`（agentId+offset）去重，但该集合在 `_clearDispatchState` 时被清空。

**结论**：轻量方案需显式说明去重依赖，且去重集合生命周期需覆盖重连窗口。

---

## 一、如何使用本矩阵

1. **按原则认领**：每个原则是一个认领单元，对应一类根因。一组人领一个原则，把这组问题一次性改透。
2. **看依赖列**：部分问题在实施上有文件/时序依赖（标注于第三节），同批改成本更低。
3. **产品决策项**：标注 ⚑ 的问题需先确认产品意图，再定方案。
4. **死代码项**：标注 ✗ 的是纯清理，不涉及原则，附在末尾顺手处理。
5. **校准项**：标注 ⚡ 的问题已结合项目实际调整优先级或方案，详见"零·一"节。

---

## 二、总览矩阵

| 原则 | 问题编号 | 一句话根因 | 严重度 |
|------|----------|-----------|--------|
| **P1 显式所有权与生命周期配对** | P0-2 | 切换会话不清 dispatching 状态 | 🔴 |
| | P0-4 ⚑ | direct 模式硬屏蔽 announce，模式边界状态转移断裂 | 🔴 |
| | P1-2 | WorkspacePicker hide() 漏 removeEventListener | 🟡 |
| | P1-5 | 模态框异步 then 不校验 activeModal | 🟡 |
| | P2-2 | 附件 input 的 change 监听登记后不移除 | 💭 |
| | P2-4 | resize 监听裸 removeEventListener 不删记录 | 💭 |
| | P3-2 | endStreaming 不清 _debounceTimer | 💭 |
| **P2 存储/发送/渲染三视图分离** | P0-1 ⚡ | 图片 dataUrl 每轮重发（必修）；历史截断降级（Gateway 已兜底） | 🟡 |
| | P1-1 | 流式每周期对累积全文跑 marked+DOMPurify | 🔴 |
| | P3-1 | 每个 delta 同步 scrollToBottom 强制 layout | 💭 |
| **P3 数据结构匹配并发模型** | P0-3 ⚡ | session-sync 单变量槽位（实际影响被高估，主 Agent 统一读取） | 🟡 |
| | P1-4 | dispatch 完成裸 setTimeout 清共享集合，无 id | 🟡 |
| **P4 异步操作必须可取消 + 世界校验** | P1-3 | Api.chat 无 idle timeout，reader.read() 永久 await | 🔴 |
| | P1-6 | throw + finally 清掉上一个请求的 abortController | 💭 |
| | P2-3 | ws-client timeout 未存 id + close 不清 pendingRequests | 💭 |
| | P2-5 | 动态加载 marked.js 无 onerror，Promise 永久 pending | 💭 |
| **P5 跨通道可靠性：至少一次 + 原子写** | P0-6 ⚡ | SSE broadcast fire-and-forget + offset 无条件推进 | 🟡 |
| | P0-5 | Agent 工作目录 rename 与配置写非原子 | 🟡 |
| **附·清理项** | P2-1 ✗ | VirtualList 397 行死代码，含假二分算法 | 💭 |

---

## 三、按原则展开的认领卡

### 认领卡 A — 原则 1：显式所有权与生命周期配对

**根因统述**：状态、监听器、定时器、异步回调没有明确所有者；所有者销毁/切换时派生资源无人清理，或异步回调不校验「世界是否仍是发起时的世界」。

| # | 问题 | 根因映射 | 修复方案 | 涉及文件 |
|---|------|---------|---------|---------|
| P0-2 | 切换不清理 dispatching | `createSession`/`selectSession`/`exitAgentMode` 三个切换入口各自只记得 `endStreaming`，漏了 `_clearDispatchState` | 统一收敛：任一 `currentSessionId` 变更都走同一套清理流程（dispatching=false + abort + clear 集合 + clear timer），写成 `SessionManager._beforeSwitch()` 契约方法，三入口强制调用 | chat-controller.js, session-manager.js |
| P0-4 ⚑ | direct 屏蔽 announce | direct 模式下「流结束 → 等待子 Agent announce」这个状态转移是断裂的，硬 return | **先定产品意图**：direct 是否支持 Agent 自主编排？若支持 → 流结束后检测是否有 pending 子 Agent，转入 dispatch-like 等待态渲染 announce；若不支持 → Agent 配置侧禁止 spawn，避免静默丢弃 | chat-controller.js |
| P1-2 | WorkspacePicker 泄漏 | `show()` 绑 document click，`hide()` 只删 panel | `hide()` 补 `document.removeEventListener('click', this._outsideClickHandler)`；对照 model-picker.js 已有正确写法 | workspace-picker.js |
| P1-5 | 模态框异步复活 | `fetchAgentDetail` then 链不校验 `State.activeModal` | 每个 then 首行加 `if (State.activeModal !== 'edit-agent' \|\| State.editingAgent !== agentId) return;`；建议封装 `_guardActive(agentId)` 工具复用 | agent-modal.js |
| P2-2 | 附件 _handlers 只增 | `fileInput.remove()` 后 _handlers 仍持引用 | change 回调内手动 removeEventListener 并从 _handlers 过滤；或一次性 input 不登记到 _handlers | interaction-bindings.js |
| P2-4 | resize 记录残留 | `_onResizeEnd` 裸 removeEventListener 不删 _handlers 记录 | resize 临时监听不走 _addListener 登记，或 _onResizeEnd 同步过滤 _handlers | interaction-bindings.js |
| P3-2 | endStreaming 漏清 timer | 只 cancel _rafId，漏 _debounceTimer | 补 `clearTimeout(this._debounceTimer)` | stream-renderer.js |

**认领建议**：1 人，半天。核心是 P0-2 的 `_beforeSwitch` 收敛和 P0-4 的产品决策；其余 5 项是配对清理，扫一遍即完。
**验收标准**：反复切换会话/开关模态框/点附件按钮 50 次，`document` 上 click 监听器数不增长；调度中途切会话后新会话能正常发消息。

---

### 认领卡 B — 原则 2：存储/发送/渲染三视图分离

**根因统述**：没有区分「完整历史（存储）」「发给模型的上下文（发送）」「屏幕增量（渲染）」三套视图，导致存储直接灌发送、存储直接灌渲染。

| # | 问题 | 根因映射 | 修复方案 | 涉及文件 |
|---|------|---------|---------|---------|
| P0-1 ⚡ | Token 失控（已校准） | 图片 dataUrl 持久化后每轮重发；历史全量发送（Gateway 已有 Pruning+Compaction 兜底） | **拆成两半，优先级不同**：① 图片 dataUrl 剥离（**必修**）：`saveUserMessage` 中不存 dataUrl，只存 path；`buildMultimodalContent` 中 dataUrl 分支删除，只用 `image_url:{url:origin+path}`（代码已有此分支）；② 历史截断（**可选优化**）：Gateway 的 ContextPruning 已自动修剪工具结果，Compaction 已自动压缩长对话。客户端截断只是减少网络传输量，不紧急。如要做，用字符数/4 粗估 token（中英文混合约 1 token ≈ 3-4 字符），设阈值截断保留最近 N 轮，不引入外部库 | message-builder.js, session-interaction.js |
| P1-1 | 流式全文重解析 | 存储=渲染：每个 debounce 周期对累积全文跑 marked+DOMPurify，缓存流式不命中 | 流式期间用轻量渲染（纯文本+代码块识别），`endStreaming` 跑一次完整渲染收敛（已有此逻辑）；或按文本长度桶做粗粒度缓存，仅增量超阈值才重解析 | stream-renderer.js, render.js |
| P3-1 | 流式滚动未合并 | 渲染视图调度不统一：渲染走 rAF 合并，滚动却每个 delta 同步 scrollTo | 把滚动并入 `scheduleRender` 的 rAF 回调，渲染完再滚到底，两者同频 | chat-controller.js, utils.js |

**认领建议**：1-2 人，1 天。P1-1 和 P3-1 强依赖（都改渲染调度），必须同批；P0-1 图片剥离独立且优先级最高，历史截断可后续再做。
**验收标准**：图片发送后后续轮次不再重发 dataUrl（抓包验证）；2000+ 字流式输出不掉帧；滚动不抖动。
**关键设计点**：明确三个数据结构边界——`session.messages`（存储，完整）、`buildApiMessages` 返回值（发送，Gateway 会修剪）、`StreamRenderer._streamState`（渲染，增量）。

---

### 认领卡 C — 原则 3：数据结构匹配并发模型

**根因统述**：用单一变量/裸定时器表达并发集合或操作共享状态，无法承载并发回传。

| # | 问题 | 根因映射 | 修复方案 | 涉及文件 |
|---|------|---------|---------|---------|
| P0-3 ⚡ | session-sync 单槽位（已校准） | `_pendingSessionKey` 单变量，并发 sessionKey 直接覆盖。**实际影响有限**：只对主 Agent session 触发，子 Agent 结果通过主 Agent session 文件统一读取 | 改为 `Map<sessionKey, {frontendSessionId}>` 队列，按 key 去重入队，逐个 `_doRead`；保留 offset 推进与 broadcast 的耦合关系（见 P5 卡 P0-6） | session-sync.js |
| P1-4 | dispatch 2s timer 串扰 | 裸 setTimeout 清共享集合，无 id，旧 timer 清空新 dispatch 状态 | 存 `this._dispatchCleanupTimer`，`_clearDispatchState` 中 clearTimeout；或改用 generation token：清理时校验当前 dispatch 代次是否仍是发起时 | chat-controller.js |

**认领建议**：1 人，半天。两题都是「单变量/裸 timer → 集合/带 id」的标准改造。
**验收标准**：主 Agent 同时 spawn 3+ 子 Agent 并发回传，无 announce 丢失；连续两次智能调度间隔 <2s，状态栏不错乱。
**检验口诀**：处理「多个并发回传」的代码，数据结构能不能同时容纳两个？不能就是错的。

---

### 认领卡 D — 原则 4：异步操作必须可取消 + 世界校验

**根因统述**：异步链缺「取消」「超时」「回调世界校验」三件套，异常路径下请求悬挂或引用丢失。

| # | 问题 | 根因映射 | 修复方案 | 涉及文件 |
|---|------|---------|---------|---------|
| P1-3 | Api.chat 无超时 | 无 idle timeout，reader.read() 永久 await | 加空闲超时（非总时长）：每次 `reader.read()` 后重置定时器，N 秒（建议 30-60s）无 chunk 则 abort；用 `AbortSignal.timeout` 或手动重置 | api.js |
| P1-6 | throw+finally 缺陷 | throw 触发 finally，清掉上一个请求的 abortController | 每个请求独立 controller（局部变量），不共享 `this._abortController` 做「占用检测」；占用检测改为 `if (busy) return onError()` 不 throw；finally 只清自己的 controller | api.js |
| P2-3 | ws-client 取消缺陷 | timeout 未存 id（无法 clear）+ close 不清 pendingRequests | ① sendRequest 存 timer id，resolve/reject 时 clearTimeout；② `ws.on('close')` 遍历 pendingRequests 全部 reject 并清空 | ws-client.js |
| P2-5 | marked 加载无 onerror | script.onerror 未设，加载失败 Promise 永久 pending | 补 `s.onerror = resolve`（降级，render.js 已有 marked undefined 分支）或 reject 后走降级渲染 | app.js |

**认领建议**：1 人，半天。P1-3 和 P1-6 同改 api.js，必同批；P2-3、P2-5 各自独立。
**验收标准**：模拟 Gateway hang 住 60s 无数据 → 客户端自动 abort 并恢复 streaming=false；手动停止上一个请求仍可生效；WS 断连后 pending 请求秒级 reject 而非等满 timeout。

---

### 认领卡 E — 原则 5：跨通道可靠性：至少一次 + 原子写

**根因统述**：跨进程/跨步骤传递无「断了怎么办、错了怎么回滚」语义——消息通道 fire-and-forget，多步写无事务。

| # | 问题 | 根因映射 | 修复方案 | 涉及文件 |
|---|------|---------|---------|---------|
| P0-6 ⚡ | SSE 断连丢消息（已校准） | broadcast fire-and-forget + offset 无条件推进（即便无客户端） | ① 轻量：broadcast 时检查 `clients.length`，为 0 则不推进 offset，等重连重读。**注意**：重连后重读会产生重复消息，需依赖前端 `_announcedOffsets`（agentId+offset）去重，且去重集合生命周期需覆盖重连窗口（不在 `_clearDispatchState` 中清空，或改用持久化 offset）；② 中量：后端环形缓冲最近 N 条事件，前端重连带 `Last-Event-ID` 补拉，按 id 幂等去重。建议先做①止血，②作为长期方案 | sse-manager.js, session-sync.js, ws-bridge.js |
| P0-5 | 目录 rename 非原子 | 先 rename 目录再写配置，中间崩溃则配置指向已不存在的旧路径 | 调整顺序：先写配置（含新 workspace 路径，tmp+rename 原子）成功后，再 rename 目录；rename 失败则回滚配置；workspace 路径替换改用 path 精确替换而非字符串 replace | agent-routes.js |

**认领建议**：1-2 人，1 天。P0-6 与认领卡 C 的 P0-3 在 session-sync.js 强耦合（都动 offset/读取链路），**建议同批或密切协调**；P0-5 独立。
**验收标准**：SSE 断连 10s 后重连，期间产生的 announce 不丢且不重复；Agent rename 中途 kill 进程，重启后配置与目录一致。

---

### 附·清理项（不涉及原则）

| # | 问题 | 处理 | 文件 |
|---|------|------|------|
| P2-1 ✗ | VirtualList 死代码 + 假二分算法 | 全项目无引用，确认短期不启用即删除；若计划启用需先修算法（前缀和数组替代二分内累加）并改 DOM diff | virtual-list.js |

**认领建议**：任意人，5 分钟。删文件即可。

---

## 四、实施批次与依赖

按「文件聚合 + 时序依赖」排成 4 个批次，建议按序推进。每批结束做一轮回归。

### 批次 1 — 止血速修（零风险，几行代码）
**目标**：清理所有「配对遗漏」类问题，立即提升健壮性。
- P1-2 WorkspacePicker removeEventListener
- P1-5 模态框 activeModal 校验
- P2-1 删 VirtualList 死代码
- P2-5 marked.js onerror
- P3-2 endStreaming 清 timer
- P2-2 附件 _handlers 清理
- P2-4 resize 记录清理

**依赖**：无。可并行。
**预计**：半天，1 人。
**回滚点**：修复前 git commit + push，出问题直接 `git reset --hard`。

### 批次 2 — 状态机与并发（核心协作可靠性）
**目标**：修复多 Agent 协作的状态一致性。
- P0-2 切换清理 dispatching（认领卡 A）
- P1-4 dispatch timer 竞态（认领卡 C）
- P0-3 session-sync 队列化（认领卡 C）
- P0-6 SSE 至少一次（认领卡 E）← 与 P0-3 同改 session-sync.js，**必同批**
- P0-4 direct 模式 announce（认领卡 A）← **需先定产品意图**

**依赖**：P0-3 ↔ P0-6 文件强耦合；P0-2、P1-4、P0-4 都动 chat-controller 状态机，建议同一人统筹。
**预计**：1.5 天，1-2 人。
**前置**：P0-4 产品决策。
**回滚点**：修复前 git commit + push，出问题直接 `git reset --hard`。

### 批次 3 — 渲染与 Token 经济（高频路径体验 + 成本）
**目标**：解决流式卡顿和图片重发。
- P1-1 流式增量渲染（认领卡 B）
- P3-1 流式滚动合并（认领卡 B）← 与 P1-1 同改渲染调度，**必同批**
- P0-1 图片 dataUrl 剥离（认领卡 B）← **必修**，Gateway 管不了
- P0-1 历史截断 ← **可选**，Gateway 已有 Pruning+Compaction 兜底，后续按需做

**依赖**：P1-1 ↔ P3-1 强耦合；P0-1 图片剥离独立且优先级最高。
**预计**：1 天，1-2 人。
**回滚点**：修复前 git commit + push，出问题直接 `git reset --hard`。

### 批次 4 — 异步可取消 + 数据原子（故障可恢复）
**目标**：异常不留僵尸请求和数据损坏。
- P1-3 Api.chat idle timeout（认领卡 D）
- P1-6 abortController 重构（认领卡 D）← 与 P1-3 同改 api.js，**必同批**
- P2-3 ws-client 取消清理（认领卡 D）
- P0-5 目录 rename 顺序调整（认领卡 E）

**依赖**：P1-3 ↔ P1-6 同文件；P0-5 独立。
**预计**：1 天，1 人。
**回滚点**：修复前 git commit + push，出问题直接 `git reset --hard`。

---

## 五、修复后在哪些维度获得显著改善

修复不是"改 bug"，而是让产品在 5 个维度真正成立。每个维度说清楚"修完是什么状态"和"业务意义"：

### 维度 1：多 Agent 协作的可靠性（产品根基）
**覆盖**：P0-2 dispatching 状态残留、P0-3 单槽位丢 announce、P0-4 direct 模式硬屏蔽、P1-4 dispatch timer 串扰。
**修完后**：并发 spawn 子 Agent 不丢消息、切换会话不串状态、私聊模式也能让 Agent 自主编排。
**业务意义**：这是"多 Agent 协作"四个字能否名副其实的核心——目前这套机制在并发和切换场景下是脆的，修完才算真的成立。

### 维度 2：长会话的经济性（能不能真用起来）
**覆盖**：P0-1 图片 dataUrl 每轮重发（必修）、历史截断（可选，Gateway 已兜底）。
**修完后**：图片不再每轮重发，会话成本曲线从"线性爆炸"变成"边际可控"。
**业务意义**：图片 dataUrl 重发是 Gateway 管不了的问题，必修。历史截断有 Gateway 的 ContextPruning+Compaction 兜底，不是上生产的前置条件。

### 维度 3：流式交互的流畅度（第一眼的专业感）
**覆盖**：P1-1 全文重解析、P3-1 同步 scroll 强制 layout、P3-2 timer 残留。
**修完后**：长输出不掉帧、不卡顿，滚动跟手。
**业务意义**：AI 应用最高频的热路径，体感问题用户一眼能看出来。目前是"渲染合并了、滚动没合并"+"全量重解析"两套问题叠加。

### 维度 4：状态与资源的健壮性（长时间运行不退化）
**覆盖**：P1-2 WorkspacePicker 泄漏、P1-5 模态框复活、P2-2 _handlers 只增不减、P2-4 resize 残留。
**修完后**：开半天内存平稳，反复开关组件不累积监听器，关闭的模态框不会异步"复活"。
**业务意义**：轻量但持续的泄漏，单次无感、长会话累积。

### 维度 5：故障可恢复性（异常不留僵尸）
**覆盖**：P1-3 Api.chat 无超时、P1-6 abortController 丢失、P0-6 SSE 断连丢消息、P2-3 ws-client close 不清 pending。
**修完后**：Gateway 重启、网络抖动、用户中途停止都不会留下"永久 streaming"或僵尸请求。
**业务意义**：目前几条通道都缺"断了怎么办"的语义。

**副产物**：P2-1 删掉 397 行死代码（VirtualList 含假二分算法）后，新人接手不会被误导，算法错误也不会被当成"已有实现"误用。属于代码可演进性，建议和上述修复一并清理。

---

## 六、关键设计契约（跨批次通用）

无论认领哪张卡，以下契约必须遵守，确保修复整体一致。这些不是新加的约束，而是把项目已经在做对的事情（pipe 背压、事件委托、原子配置写）推广到所有路径——团队不需要推倒重来，而是补齐一致性。

### 契约 1：显式所有权与生命周期配对
- 每个状态、监听器、定时器、pending 请求都必须有唯一所有者（通常是某个 controller 或组件实例）。
- 所有者的销毁/切换路径必须强制释放所有派生资源，写成 `destroy()` / `cleanup()` 契约方法，不允许"创建处记得、销毁处忘了"。
- 状态切换 = 旧所有者完整销毁 + 新所有者完整初始化，不存在跨生命周期的残留。`createSession` / `selectSession` / `exitAgentMode` 都必须走同一套清理流程，不是各自记得调一下。

### 契约 2：存储 / 发送 / 渲染三视图分离
- **存储视图**：完整消息历史，含图片 base64，用于持久化和回看。
- **发送视图**：按 token 预算截断（可选，Gateway 已有 Pruning 兜底）、图片 dataUrl 替换为路径引用（必修）、系统提示词去重。这是 `message-builder` 的唯一职责。
- **渲染视图**：流式期间只对新增片段做增量解析，全文渲染仅在流结束后做一次收敛。`stream-renderer` 的 debounce 缓存必须对"未变化的已渲染前缀"命中。
- 三视图各有数据结构，互不直接耦合。任何"直接拿 `session.messages` 发 API""直接拿累积文本跑 marked"的代码都属于违反本原则。

### 契约 3：数据结构匹配并发模型
- 多生产者场景必须用 `Map<key, Set<item>>` 或队列，明确键和去重策略，禁止用单变量槽位承载并发。
- 任何"延迟清理集合"的操作必须带唯一 id，清理时校验 id 是否仍是当前 owner，禁止裸 setTimeout 操作共享状态。
- **一句话检验**：如果一段代码处理"多个并发回传"，它用的数据结构能不能同时容纳两个？不能就是错的。

### 契约 4：异步操作必须可取消 + 世界校验
- 每个异步操作（fetch、WS 请求、定时器回调）必须有 cancel token / AbortController / timeout 三件套，缺一不可。
- `throw` 和 `finally` 不能组合使用来清理共享引用——throw 路径会执行 finally，清掉不属于自己的引用。用"每个请求独立 controller"代替"共享 controller + finally 清空"。
- 所有异步回调的第一行必须校验"世界是否仍是发起时的世界"：模态框是否还开着、会话 id 是否未变、组件是否未销毁。用 generation token 或 activeRef 实现，不要靠"概率低所以不管"。

### 契约 5：跨通道可靠性——至少一次 + 原子写
- 消息通道（SSE / WS / HTTP）一律按"至少一次"设计：消息带 id + offset，客户端断连重连后可按 offset 补拉，重复消息按 id 幂等去重。fire-and-forget 仅允许在"丢了也能容忍"的场景（如日志、调试事件），业务消息禁止。
- 多步写操作必须原子或可回滚：临时目录 + rename、预写日志 + apply、或显式回滚序列，不允许出现"中间崩溃 = 数据损坏"的窗口。Agent 工作区 rename 这种"先动目录再写配置"的顺序必须反过来或包进事务。

---

## 七、一句话总结

这 5 条原则的检验标准很简单：**每一条都能在现有代码里找到已经做对的例子**——`proxy.js` 的 pipe 背压是原则 5、`interaction-bindings` 的统一登记是原则 1、`openclaw.json` 的 tmp+rename 是原则 5。所以这不是引入新范式，而是**把局部的好实践提升为全局的硬约束**，把「某些路径漏了」补成「所有路径都过这关」。团队接受度会高，因为不是推翻重写，是统一标准。

**结合项目实际的补充**：OpenClaw Gateway 已有 ContextPruning + Compaction 兜底，P0-1 的历史截断不是必须项；P0-3 的并发影响被高估，主 Agent 统一读取机制已缓解。修复时优先解决 Gateway 管不了的问题（图片 dataUrl 重发、客户端状态机、渲染性能），不重复造 Gateway 已有的轮子。

---

*编制：CodeReviewExpert · 基于两轮审查报告*
*校准：虾指挥团队 · 结合 OpenClaw 原生能力 + 项目实际*
