# 第二轮代码审查报告 — 运行时风险与原生 JS 细节（P1/P2/P3）

> 审查范围：内存泄漏、流式渲染性能、后端代理健壮性、竞态条件、异常边界、代码质量、用户体验 bug
> 明确忽略：安全问题、可访问性、TypeScript 迁移、测试覆盖、文档缺失
> 审查日期：2026-06-17

---

## 总体评价

第二轮钻进运行时细节后，结论是：**通信层和资源清理的骨架相当扎实，问题集中在渲染热路径和异步竞态的边界处理上**。

值得肯定的设计：
- `interaction-bindings.js` 用 `_addListener` 包装器统一记录所有 handler，`destroy()` 可全量回收——这是原生 JS 项目里少见的事件管理规范。
- `ws-bridge.js` 的 SSE 重连指数退避 + `destroy()` 完整清理（reconnectTimer、source、listeners）。
- `ws-client.js` 的三套定时器（reconnect/ping/sessionPoll）在 `disconnect()` 里全数清理，sessionPoll 还是 subscribe 失败的降级方案，设计周全。
- `proxy.js` 用 `gwRes.pipe(res)`（pipe 自带背压）+ `res.on('close') → gwReq.destroy()`（客户端断连销毁上游）——流式转发的背压和资源释放都到位。
- `render.js` 的 Markdown LRU 缓存 + 流式期间不写缓存，思路正确。

问题分布在三个面：**流式渲染的全量重解析开销**、**几处 document 级监听器遗漏清理**、**异步链缺少状态校验导致的竞态**。按优先级如下。

---

## P1 — 功能性风险（可能导致运行时异常或显著性能问题）

### P1-1 流式渲染全文重解析，主线程阻塞【高频路径】

**位置**：`web/js/components/stream-renderer.js` `renderBubble()` + `web/js/utils/render.js` `renderMarkdown()`

**问题**：
```js
// stream-renderer.js:70 — 流式期间每个渲染周期
contentEl.innerHTML = renderMarkdown(st.text, true); // streaming=true

// render.js:65 — streaming=true 时缓存永不命中
function renderMarkdown(text, streaming) {
  const cached = _mdCache.get(text);
  if (cached) return cached;
  // ...
  let html = marked.parse(text, { breaks: true, gfm: true, renderer: ... });
  html = DOMPurify.sanitize(raw, { ... });
  if (!streaming) { _mdCache.set(text, html); ... }  // 流式不写缓存
  return html;
}
```

流式期间 `streaming=true`，`_mdCache` 永远 miss，每个 debounce 周期（rAF + 50ms）都对**累积全文**跑一次完整 `marked.parse`（同步）+ `DOMPurify.sanitize`（同步），然后全量 `innerHTML` 替换。

**影响**：AI 输出长文本（>2000 字）时，文本越长，每个 50ms 周期的解析耗时越长（marked.parse 复杂度近似 O(n)）。当单次解析超过 50ms，渲染开始积压掉帧，表现为流式输出明显卡顿。这是整个应用最高频的渲染热路径。

**修复方向**：
- 流式期间降低渲染频率：将 debounce 从 50ms 提到 80–120ms，或改为按字符增量阈值触发（每累积 N 字符渲染一次）。
- 流式期间用轻量渲染：不跑完整 marked + DOMPurify，只做纯文本 + 代码块识别，`endStreaming` 时再跑一次完整渲染（已有此逻辑，见 `endStreaming` 的 `renderMarkdown(st.text, false)`）。
- 或对流式中间结果做粗粒度缓存：以文本长度的桶（如每 256 字符一档）做 key 缓存上一次解析结果，仅当增量超过阈值才重解析。

---

### P1-2 WorkspacePicker 的 document click 监听器泄漏【确凿泄漏】

**位置**：`web/js/components/workspace-picker.js`

**问题**：
```js
// _bindEvents:74 — show 时绑定
setTimeout(function () {
  document.addEventListener('click', self._outsideClickHandler);
}, 0);

// hide:25 — 关闭时只移除 panel，不移除 document 监听
hide: function () {
  if (this._panel) {
    this._panel.remove();
    this._panel = null;
  }
  // ← 缺少 document.removeEventListener('click', this._outsideClickHandler)
}
```

每次 `show()` → `hide()`，`document` 上累积一个永远不会移除的 `click` handler。`_outsideClickHandler` 内部 `if (!panel) return` 虽能避免误触发，但监听器本身不释放，且持有对 `WorkspacePicker` 对象的引用（通过 `self`/`this`）。

**影响**：长期反复打开-关闭工作目录选择器，`document` 上的 click 监听器线性增长，每次点击遍历所有 handler（多数 early return）。对比 `model-picker.js` 有正确的 `removeEventListener`，此处是遗漏。

**修复**：
```js
hide: function () {
  if (this._panel) { this._panel.remove(); this._panel = null; }
  document.removeEventListener('click', this._outsideClickHandler);
}
```

---

### P1-3 流式请求无客户端超时，Gateway hang 住则永久卡死

**位置**：`web/js/api.js` `Api.chat()`

**问题**：
```js
chat: async function (messages, agentId, callbacks) {
  // ...
  this._abortController = new AbortController();
  // 没有 setTimeout 触发 abort
  const res = await fetch('/v1/chat/completions', { ..., signal: this._abortController.signal });
  // ...
  const reader = res.body.getReader();
  while (true) {
    const result = await reader.read();  // ← 若 Gateway 不发 [DONE] 也不断开，永久 await
    // ...
  }
}
```

`chat()` 用 `AbortController` 但不设超时。若 Gateway 流式响应中途 hang 住（网络抖动、上游 Agent 卡死不发数据也不断开），`reader.read()` 永久 await，`State.streaming` 永远 `true`。仅靠 `server.js` 的 `server.timeout = 180000`（3 分钟 socket 超时）兜底，但那是 socket 级别，且对长 AI 响应可能误杀。

**影响**：用户卡在 streaming 状态，无法发送新消息（`sendMessage` 开头 `if (State.streaming) return`）。用户可手动点停止按钮（`stopGeneration` 调 `abortController.abort()`），但若不主动操作则永久卡死，且无任何超时提示。

**修复方向**：为 `chat()` 增加空闲超时（idle timeout）——不是总时长超时（流式可能很长），而是「两个 chunk 之间超过 N 秒无数据」则 abort。可用 `AbortSignal.timeout` 或手动在每次 `reader.read()` 后重置定时器。

---

### P1-4 dispatch 完成 2 秒延时清空集合的竞态

**位置**：`web/js/controllers/chat-controller.js` `_checkDispatchComplete()`

**问题**：
```js
_checkDispatchComplete: function () {
  // ... 判定完成 ...
  State.setState({ dispatching: false });
  // ...
  const self = this;
  setTimeout(function () {            // ← 无 id，无法取消
    self._activeSubagents.clear();
    self._completedSubagents.clear();
    self._announcedOffsets.clear();
    self._progressElements = {};
  }, 2000);
}
```

该 `setTimeout` 没有保存 id。若 2 秒内用户发送新消息触发新的智能调度，`sendMessage` 开头的 `_clearDispatchState()` 会先清空集合，新 dispatch 开始后 `_activeSubagents` 被加入新子 Agent。但 2 秒后旧 timer 触发，会**清空新 dispatch 的状态集合**。

**影响**：连续两次智能调度（间隔 < 2 秒）时，第二次调度的 `_activeSubagents`/`_completedSubagents` 被旧 timer 清空。此后 `_checkDispatchComplete` 判定 `completed(0) >= active(0)` 恒真，新 dispatch 会误判为立即完成，状态栏/按钮状态错乱。好消息是 `handleAnnounceResult` 不依赖 `dispatching` 状态（只检查 `interactionMode !== 'direct'`），后续 announce 仍能渲染，不会丢消息——但 UI 状态会错。

**修复**：保存 timer id，`_clearDispatchState` 中一并 `clearTimeout`：
```js
_checkDispatchComplete: function () {
  // ...
  var self = this;
  this._dispatchCleanupTimer = setTimeout(function () { ... }, 2000);
},
_clearDispatchState: function () {
  // ...
  if (this._dispatchCleanupTimer) { clearTimeout(this._dispatchCleanupTimer); this._dispatchCleanupTimer = null; }
}
```

---

### P1-5 编辑模态框异步竞态——关闭后表单"复活"

**位置**：`web/js/components/agent-modal.js` `_showEdit()`

**问题**：
```js
_showEdit: function () {
  var agentId = State.editingAgent;
  // ...
  var self = this;
  Api.fetchAgentDetail(agentId).then(function (detail) {       // 异步
    Api._fetch(...'/tools-md').then(function (td) {             // 异步嵌套
      // ... 此处未校验 State.activeModal 是否仍为 'edit-agent'
      self._renderEditForm(agent, agentId, detail, userPart, sysPart);
    });
  });
}
```

`fetchAgentDetail` + `tools-md` 是异步链。若用户在 fetch 进行期间关闭模态框（`State.activeModal = null`）或切换到创建模式，fetch resolve 后 `_renderEditForm` → `_renderForm` 仍会 `document.body.appendChild(overlay)`，导致**用户关闭后表单又冒出来**。

**影响**：快速关闭编辑框（或编辑 A 时点击新建 B）时，A 的表单在关闭后异常重现，盖在 B 之上或单独出现，造成混乱。

**修复**：在 then 回调里校验状态：
```js
Api.fetchAgentDetail(agentId).then(function (detail) {
  if (State.activeModal !== 'edit-agent' || State.editingAgent !== agentId) return; // 已关闭或已切换
  // ...
});
```

---

### P1-6 Api.chat 的 abortController throw + finally 组合缺陷

**位置**：`web/js/api.js` `Api.chat()`

**问题**：
```js
chat: async function (messages, agentId, callbacks) {
  if (this._abortController) {
    throw new Error('Previous request still in progress');  // ← throw
  }
  this._abortController = new AbortController();
  try {
    // ...
  } catch (err) {
    // ...
  } finally {
    this._abortController = null;   // ← throw 时也会执行，清掉上一个请求的 controller
  }
}
```

当上一个请求仍在进行（`_abortController` 非空）时进入 `chat()`，`throw` 后 `finally` 执行 `this._abortController = null`——这会**清掉上一个仍在进行的请求的 abortController 引用**。之后用户点停止按钮，`stopGeneration()` 里 `if (this._abortController)` 为 null，无法中止上一个请求。

**影响**：依赖 `State.streaming` 状态不一致才触发（正常情况下 `sendMessage` 开头的 `if (State.streaming) return` 会拦住）。一旦 streaming 状态与 abortController 不同步（如 `onError` 路径的时序窗口），上个请求将无法被停止。概率低，但设计有缺陷。

**修复方向**：`throw` 前不要进入会触发 `finally` 的路径，或改为不 throw 而是返回/调用 onError；也可在 `finally` 里只在请求真正完成时才清空。

---

## P2 — 代码质量与优化

### P2-1 VirtualList 是 397 行死代码，且含算法错误

**位置**：`web/js/components/virtual-list.js`

全项目除自身定义外**无任何引用**（已全局搜索确认）。纯死代码。且若将来启用存在两个问题：
1. `_getIndexAtOffset` 注释声称「二分查找 O(log n)」，但二分的每一步内有 `for (let i = 0; i < mid; i++) midOffset += ...` 线性累加，实际 O(n log n)。
2. `_renderVisible` 每次滚动都 `innerHTML = ''` 全量重建可见 DOM（含 topSpacer + 消息 + bottomSpacer），频繁触发重排。

**建议**：当前消息列表直接用 `MessageRenderer.appendMessage` 追加，未走虚拟滚动。若短期内不计划启用，删除该文件减少维护负担；若计划启用，需先修复算法（用前缀和数组替代二分内累加）并改用 DOM diff 而非全量重建。

---

### P2-2 附件按钮 _handlers 数组只增不减

**位置**：`web/js/ui/interaction-bindings.js` `_bindButtons()` attach-btn

```js
self._addListener(attachBtn, 'click', function () {
  const fileInput = document.createElement('input');
  // ...
  self._addListener(fileInput, 'change', function () {  // ← push 到 _handlers
    // ...
    fileInput.remove();                                   // ← DOM 移除，但 _handlers 仍持有引用
  });
  document.body.appendChild(fileInput);
  fileInput.click();
});
```

每次点附件按钮，`_addListener` 把 `{element: fileInput, event: 'change', fn: ...}` push 到 `_handlers`。`fileInput.remove()` 后元素脱离 DOM，但 `_handlers` 数组仍持有该元素引用，阻止 GC。频繁使用附件按钮，`_handlers` 缓慢增长。

**修复**：change 回调内手动 `fileInput.removeEventListener('change', ...)` 并从 `_handlers` 移除该条；或附件 input 用一次性匿名监听器不登记到 `_handlers`。

---

### P2-3 ws-client sendRequest timeout 未存 id + close 不清 pendingRequests

**位置**：`ws-client.js`

```js
function sendRequest(method, params, timeoutMs) {
  return new Promise(function (resolve, reject) {
    // ...
    pendingRequests[id] = { resolve: resolve, reject: reject };
    ws.send(...);
    setTimeout(function () {            // ← 未存 id
      if (pendingRequests[id]) { delete pendingRequests[id]; reject(...); }
    }, timeoutMs || 30000);
  });
}

ws.on('close', function () {
  // ← 未清理 pendingRequests，已发请求等各自 timeout 才 reject
});
```

两点：① timeout 的 `setTimeout` 未存 id，请求正常完成后该 timer 仍会跑（虽 `if (pendingRequests[id])` 跳过，无害但浪费）；② `ws.on('close')` 不清理 `pendingRequests`，断连后已发请求要等各自 timeout（最长 60s）才 reject，调用方长时间悬挂。

**修复**：`ws.on('close')` 中遍历 `pendingRequests` 全部 reject 并清空；sendRequest 里存 timer id，resolve/reject 时 clearTimeout。

---

### P2-4 agent-section resize 监听器记录残留

**位置**：`web/js/ui/interaction-bindings.js` `_onResizeStart` / `_onResizeEnd`

```js
function _onResizeStart(e) {
  // ...
  self._addListener(document, 'mousemove', _onResizeMove);   // ← push 到 _handlers
  self._addListener(document, 'mouseup', _onResizeEnd);
  // ...
}
function _onResizeEnd() {
  // ...
  document.removeEventListener('mousemove', _onResizeMove);   // ← 裸移除，不从 _handlers 删除记录
  document.removeEventListener('mouseup', _onResizeEnd);
  // ...
}
```

`_onResizeStart` 用 `_addListener` 登记 4 条记录到 `_handlers`，但 `_onResizeEnd` 用裸 `removeEventListener` 移除，不从 `_handlers` 数组删除对应记录。每次 resize 操作，`_handlers` 多 4 条失效记录（元素是 document，不会被 GC，但记录本身是冗余对象）。`destroy()` 时会再 `removeEventListener` 一次（无害）。

**影响**：轻微，`_handlers` 随 resize 次数线性增长，但每条只是小对象。建议 `_onResizeEnd` 中同步从 `_handlers` 过滤掉对应记录，或 resize 临时监听器不走 `_addListener` 登记。

---

### P2-5 app.js 动态加载 marked.js 无失败处理

**位置**：`web/js/app.js` `init()`

```js
if (typeof marked === 'undefined') {
  await new Promise(function (resolve) {
    const s = document.createElement('script');
    s.src = '/js/lib/marked.min.js';
    s.onload = resolve;
    document.head.appendChild(s);   // ← 无 s.onerror，加载失败则 Promise 永久 pending
  });
}
```

若 `marked.min.js` 加载失败（本地部署概率极低，但网络异常/文件缺失时可能），`onload` 不触发，Promise 永久 pending，`init()` 卡死，整个应用不初始化。

**修复**：加 `s.onerror = function () { resolve(); }` 或 reject 后降级（render.js 已有 `typeof marked === 'undefined'` 的降级分支）。

---

## P3 — 用户体验 bug

### P3-1 流式输出每个 delta 同步滚动，强制 layout

**位置**：`web/js/controllers/chat-controller.js` `onDelta` + `web/js/utils/utils.js` `scrollToBottom()`

```js
// chat-controller.js onDelta
onDelta: function (text) {
  // ...
  StreamRenderer.scheduleRender();              // ← 渲染走 rAF 合并 ✓
  if (!State.userScrolledUp) {
    scrollToBottom(document.getElementById('messages'), false);  // ← smooth=false，同步滚动 ✗
  }
}

// utils.js scrollToBottom
scrollToBottom: function (el, smooth) {
  if (!smooth) {
    el.scrollTo({ top: el.scrollHeight, behavior: 'instant' });  // ← 强制 layout
    return;
  }
  // smooth 才走 rAF 合并
}
```

`stream-renderer` 的渲染已被 rAF + debounce 合并，但 `onDelta` 里每个 SSE delta 都调 `scrollToBottom(el, false)`，`smooth=false` 直接同步 `scrollTo`，读取 `scrollHeight` 并强制 layout。渲染合并了，滚动没合并——两者节奏不一致。

**影响**：高频 delta（如每 30–50ms 一个）时，每次 delta 触发一次同步滚动 + 强制 layout，与渲染的 rAF 合并错位，表现为流式输出时滚动抖动/卡顿。

**修复**：滚动也走 rAF 合并，或干脆在 `scheduleRender` 的 rAF 回调里统一做滚动（渲染完再滚到底），让渲染和滚动同频。

---

### P3-2 stream-renderer endStreaming 不清 _debounceTimer

**位置**：`web/js/components/stream-renderer.js` `endStreaming()`

```js
endStreaming: function (skipCleanup) {
  // ...
  if (this._rafId) { cancelAnimationFrame(this._rafId); this._rafId = null; }
  // ← 未 clearTimeout(this._debounceTimer)
  // ...
  this.resetStreamState();
}
```

`endStreaming` 清了 `_rafId` 但没清 `_debounceTimer`。若 endStreaming 时有 pending debounce（rAF 已触发，50ms 定时器在跑），50ms 后会触发 `renderBubble()`。此时 `resetStreamState()` 已把 `_streamState.bubble` 置 null，`renderBubble` 开头 `if (!st.bubble) return` 保护，不崩。但残留 timer 会多跑一次空函数。

**影响**：无功能影响，仅残留一个 50ms 定时器。建议补 `clearTimeout(this._debounceTimer)`。

---

## 修复优先级建议

| 优先级 | 问题 | 改动量 | 建议 |
|--------|------|--------|------|
| 🔴 立即 | P1-1 流式全文重解析 | 中 | 最高频路径，长文本卡顿明显，优先优化 |
| 🔴 立即 | P1-2 WorkspacePicker 泄漏 | 极小 | 一行 removeEventListener，零风险 |
| 🔴 立即 | P1-3 流式无超时 | 小 | 加 idle timeout，防永久卡死 |
| 🟡 尽快 | P1-5 模态框异步竞态 | 极小 | then 里加一行状态校验 |
| 🟡 尽快 | P1-4 dispatch 2s timer 竞态 | 小 | 存 id + clear |
| 🟡 尽快 | P3-1 流式滚动未合并 | 小 | 滚动并入 rAF |
| 💭 顺手 | P1-6 abortController throw | 小 | 重构 throw 逻辑 |
| 💭 顺手 | P2-1 删除 VirtualList 死代码 | 极小 | 删文件 |
| 💭 顺手 | P2-2/P2-3/P2-4 | 小 | 清理登记一致性 |
| 💭 顺手 | P2-5 marked 加载 onerror | 极小 | 加一行 |
| 💭 顺手 | P3-2 清 _debounceTimer | 极小 | 加一行 |

**性价比最高的一组**：P1-2、P1-5、P2-1、P2-5、P3-2 都是几行代码、零风险的修复，建议一并清理。P1-1 和 P3-1 是渲染体验的核心，建议一起做（都涉及流式渲染调度，可统一在 `scheduleRender` 的 rAF 回调里合并渲染 + 滚动）。

---

*审查者：CodeReviewExpert · 第二轮*
