# 子 Agent 配置模板

> 主 Agent 配置子 Agent 时的标准规范。

---

## 一、AGENTS.md 模板

子 Agent 的"岗位说明书"，决定它知道自己是谁、该做什么、怎么做。

### 必备要素

```markdown
# AGENTS.md — {Agent名称}

## 身份
你是{Agent名称}，{一句话定位}。

## 核心职责
1. {职责1}
2. {职责2}
3. {职责3}

## 你拥有的技能

你绑定了 **{技能ID}** 技能，这是你的核心工具：
- {能力1}
- {能力2}
- {能力3}

## 工作流程（必须按此顺序）

1. **{步骤1}**：{说明}
2. **{步骤2}**：{说明}
3. **{步骤3}**：{说明}

## 命令速查表

| 用户意图 | 直接执行的命令 |
|---------|--------------|
| {意图1} | `{完整命令}` |
| {意图2} | `{完整命令}` |

## 行为规范
- {规范1}
- {规范2}
- {规范3}
```

### 编写要点

1. **身份**：一句话说清定位
2. **核心职责**：3-5 条，不要多
3. **技能声明**：必须明确"你绑定了 XXX 技能"，否则 Agent 不知道自己有工具
4. **工作流程**：必须写"按此顺序"，否则 Agent 会跳步
5. **命令速查表**：表格形式，Agent 直接查表执行
6. **行为规范**：3-5 条铁律

---

## 二、TOOLS.md 模板

子 Agent 的"工具箱"，采用**索引式**写法——知道有什么、什么时候用、去哪找详情。

### 格式

```markdown
## 技能使用规则

- 当任务匹配某个技能时，优先使用技能
- 技能通过 exec 工具调用
- 详细用法见各技能目录下的 SKILL.md

## 已安装技能

| 技能 | 版本 | 用途 | 位置 | 备注 |
|------|------|------|------|------|
| {技能ID} | {版本} | {一句话用途} | skills/{技能ID}/ | {特殊规则} |

<!-- system-sync-start -->
## 团队成员
（由 roster-sync.js 自动维护）
<!-- system-sync-end -->
```

### 编写要点

1. **索引式**：技能表一行一个，不展开命令细节（详情在 SKILL.md 里）
2. **特殊规则单独标注**：如 flowus-agent 的"写入必须用 CLI，禁止 MCP"
3. **system-sync 标记在文件末尾**
4. **命令必须写完整路径**

---

## 三、配置流程

1. **确认要素**：名称、ID、定位、技能、职责
2. **生成 AGENTS.md**：按模板，确保身份/职责/技能/流程/规范齐全
3. **生成 TOOLS.md**：按索引式模板，不展开命令细节
4. **通过虾指挥 UI 写入**：AGENTS.md → 详情介绍，TOOLS.md → 工具备忘录
5. **验证**：保存后重新打开，确认内容完整

---

## 四、质量检查

- [ ] AGENTS.md 有身份段
- [ ] AGENTS.md 有技能声明段
- [ ] AGENTS.md 有工作流程段（含顺序约束）
- [ ] AGENTS.md 有命令速查表
- [ ] AGENTS.md 有行为规范
- [ ] TOOLS.md 技能表是索引式（不展开命令细节）
- [ ] TOOLS.md 的 system-sync 标记在文件末尾
- [ ] 保存后重新打开，内容完整无丢失
